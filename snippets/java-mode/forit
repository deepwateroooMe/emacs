# contributor: Renaud Aubin <renaud.aubin@gmail.com>
# name: for (Iterator ...) { ... }
# key: fit
# --
for (Iterator ${1:it} = $2.iterator(); $1.hasNext(); ) {
    ${3:String} ${4:name} = ($3)$1.next();
    $0
}
